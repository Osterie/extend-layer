#Requires AutoHotkey v2.0

; TODO create a class for making a true keyboard overlay, and change the name of the other classes with thath name.
; the keyboard overlay class should create a true keyboard overlay, which is stored in the ini file so it can be customized by the user